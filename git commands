 git init        												   //to initalize git  
 
 git config --global user.name "Umeshkmaurya197"                   //to configure user name only one time for one laptop
 git config --global user.email "umeshkumarmaurya.98@gmail.com"    //to configure email only one time for one laptop
 
 git config --global user.email                                    // to show configured email 
 git config --global user.name                                     // to show congigures username 

 git remote add origin "https://github.com/Umeshkmaurya197/Hello-Project.git"    // create connection with remote repository    

 touch hello.txt 	                                               //create only empty file  
 nano hello.txt                                                    // create and open file 
 
 cat hello.txt                                                     //show content 
 
 git add hello.txt                                                 //make the file ready for commit
 
 git add .                                                         //make all the file ready for commit
 
 git status			                                               // show git status

 git commit -m "Added: hello world Message"                        // to commit  

 git push -u origin master                                         // will ask for token  pass the below token for first time 

 tokan for git push : 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     // after use this token file will pushed to remote repository
 
 git checkout -b additionbranch                                    //create branch and open branch 
 
 git checkout master                                               //enter into master branch 
 
 git merge additionbranch                                          // merge additionbranch to master branch 
 
 git push -u origin removeaddition                                 //   push  local removeaddition to remote removeaddition branch
 
 git branch -d deleteaddition                                      //delete branch in git software : local repository
 
 git push origin --delete removeaddition                           //delete branch in git software : remote repository
     
 git pull origin master                                            // pull code from master branch : remote repository


